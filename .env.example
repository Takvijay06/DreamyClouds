# Optional: require explicit analytics consent before GA4 loads (see `src/services/analytics.ts`).
# VITE_GA_REQUIRE_CONSENT=false
#
# Measurement ID is set in code: `GA4_MEASUREMENT_ID` in `src/services/analytics.ts`.
